# 千匣台 改进计划（第五轮 · 阶段 P–T · 2026-08-28 · 待评审草案）

> 本文档是 `IMPROVEMENT_PLAN_4.md`（第四轮，阶段 L–O，QX-10~QX-14 验收通过）+
> QX-15 合并列车闭环之后的**第五轮发展规划草案**。
> 与前四轮不同，本轮不始于代码核实，而始于**项目定位与战略选择**——
> 在动任何代码前，先让方向经过产品（小潘）拍板。
>
> **本文档状态：已拍板（2026-08-28）。D1-D4 决策点已全部拍板**（见第 1 节 ✅ 标记），
> 阶段 Q-R-S-T 工单（QX-20~31）已注册 `docs/WORK_ORDERS.md` 第六轮工单节；阶段 P（QX-16~19）先行。
>
> 当前基线（2026-08-27）：`cargo test --workspace` desktop bin **271 passed（1 ignored）** /
> installer **30 passed** 全绿；vitest contract **37** / platform-contract **34** /
> plugin-sdk **163** / desktop **69** 全绿；四包 typecheck 干净；`main` 与 `origin/main` 同步。
> 第四轮 6 分支已并入 main（QX-15 合并列车）；第五轮工单池 QX-16~QX-19 已登记未派发。

---

## 0. 定位校准（撰写本规划的唯一依据）

来源：产品（小潘）2026-08-28 拍板的三项约束。

| # | 约束 | 含义 |
|---|---|---|
| 1 | **时间跨度：3-6 个月短期** | 不做跨平台、不做 v2 架构演进、不做大规模重构；投资必须 6 个月内见效 |
| 2 | **定位：求职作品 + 个人工具** | 技术深度做满能讲故事即可；不追求商业化、不追求付费转化；求职/作品集可量化是硬指标 |
| 3 | **突破方向：真实用户与分发** | 破零真实用户、从自用走向他用；GitHub Release 是现成分发渠道 |

### 由三项约束推导出的战略边界（不可破）

- **不引入轻量云**：零服务器是项目叙事灵魂，破云即破灵魂。任何"托管市场 / 计费 / 账号体系"都违背约束 2。
- **不做跨平台**：v1 Windows 收尾即够；macOS 移植 4 个硬骨头（Job Object 沙箱 / SFX 安装器 / WebView2 / rc.exe）单独评估，3-6 个月内不排期（延续 H2 ADR）。
- **不建插件市场**：3-6 个月时间不够建市场+审核流+生态飞轮；分发只走 GitHub Release + 用户本地导入。
- **不追求付费**：约束 2 明示。任何付费墙、增值功能都不在本轮范围。
- **不扩能力面**：17 kind 已够覆盖目标场景；新 kind 直到真实插件需求出现才加（延续 H3）。

### 战略押注（一句话）

> **把"技术深度已足、零真实用户"的现状，转成"GitHub 上有可装可用的产品 + 3-5 名种子用户每周在用 + 一篇能讲清架构决策的博客系列"。**
> 押注三件事：**分发门面**（让陌生人愿意点开）、**真用插件**（自己每天用逼出真摩擦）、**真实用户冷启动**（找 3-5 人内测）。

---

## 1. 关键决策点（⚠️ 待小潘拍板，未拍板不得派发后续工单）

### ✅ D1 · 垂直场景定锚（2026-08-28 已拍板：**A · 本地知识库 / RAG**）

**问题**：3-6 个月不可能做"通用桌面插件平台"叙事——这个叙事需要一个生态，本轮建不起。必须绑一个具体场景做深，让"千匣台能干什么"可量化、可演示。

**候选**（建议按优先级排序）：

| 候选 | 场景叙事 | 技术契合 | 摩擦来源 | 风险 |
|---|---|---|---|---|
| **A · 本地知识库 / RAG**（推荐） | "千匣台 = 本地优先的 AI 知识库工作站" | **高**：小潘技术主线（RAG 检索增强），求职叙事天然咬合；Qdrant/BM25/Cross-Encoder 重排已是小潘栈；可做"导入文档 → 向量化 → 跨插件可检索的本地知识层" | 自己每天会真用（资料整理、求职素材、技术笔记） | RAG 是个深坑，需控制范围——v1 只做"插件级 kv + 全文检索 + AI 摘要"，向量检索放 v2 |
| B · AI 内容创作工作台 | "本地优先的 AI 内容创作工作台" | 中：已有 clip-digest/web-clip 雏形 | 写作/视频脚本场景 | 拥挤赛道（Notion AI / Obsidian AI 等），差异化弱 |
| C · 截图笔记 / OCR 翻译 | "截屏即记即翻" | 低：依赖 system.screenshot + 第三方 OCR | 单点工具感 | 太轻，撑不起"工作台"叙事 |

**建议**：A 本地知识库 / RAG。理由：
1. 技术主线咬合，求职面试官一句话就能听懂"你做的是 RAG 桌面化"；
2. 本地优先 + 零服务器叙事天然契合（"你的知识库不上云"是卖点）；
3. 小潘已有 KB-AI 项目经验，复用栈而非另起；
4. 求职作品可量化维度多（检索召回率、冷启动时间、成本、并发）。

**拍板结果（2026-08-28）**：选定 **A · 本地知识库 / RAG**。v1 范围红线：导入 .md/.txt → 切片 → 关键词全文检索（storage.kv list + 客户端 JS 检索）→ LLM 问答（带检索片段 context）；**不做**向量检索 / 跨插件共享知识层 / PDF·Office 解析。

### ✅ D2 · 文档与门面投入力度（2026-08-28 已拍板：**建议口径**）

**问题**：README 重写 + Demo GIF + Landing page + 文档站——这四件哪几件做、做到什么程度？

**建议**：
- **必做**：README 重写（用户视角，3 屏内说清"是什么 / 为什么 / 怎么装"）+ 1 分钟 Demo GIF（展示垂直场景插件的真实使用）。
- **可选**：Landing page（GitHub Pages 即可，复用 docs/ 构建）。
- **不做**：独立文档站（vitepress/docusaurus）。CODEBUDDY.md + docs/*.md + README 已够，过度投资文档基建违背"停止追加基础设施"方针。

**拍板结果（2026-08-28）**：README 重写 + Demo GIF **必做**（QX-20）；Landing page（QX-21）**可选**，暂不派发，待 QX-20 落地后再定；独立文档站**不做**。

### ✅ D3 · 真实用户来源与反馈通道（2026-08-28 已拍板：**同学/朋友圈**）

**问题**：3-5 名种子用户从哪来？反馈走什么通道？

**建议候选**（多选）：
- 小潘同学/朋友（北理工珠海学院 2026 届软件工程圈，最易触达）；
- 求职过程中遇到的技术圈（GitHub / 掘金 / V2EX / 即刻发帖）；
- 小潘现有 GitHub followers（qiuuchan 公开项目已有积累）。

反馈通道：GitHub Issues（公开可追溯）+ 一个微信群（实时性强）。**不做**独立反馈系统。

**拍板结果（2026-08-28）**：触达渠道仅 **小潘同学/朋友**（北理工珠海学院 2026 届软件工程圈）；技术社区发帖与 GitHub 私域本轮不投入。反馈通道：GitHub Issues（公开可追溯）+ 一个微信群（实时）；不做独立反馈系统。

### ✅ D4 · 内容输出节奏（2026-08-28 已拍板：**仅 GitHub Discussions**）

**问题**：求职作品的"博客系列"放哪、发多少篇、什么节奏？

**建议**：
- 平台：掘金（技术圈密度高）+ GitHub Discussions（项目内深度文）+ 小潘个人博客（如有）。
- 节奏：6 个月 4-6 篇，每月 1 篇左右；不强求密度，质量优先。
- 选题骨架（待 D1 定锚后细化）：
  1. 《零服务器桌面插件平台：Tauri v2 + Job Object 沙箱的三档安全边界》
  2. 《契约驱动开发：用 Zod 当 host↔plugin 唯一权威源的真实代价》
  3. 《把 1.7GB runtime 物料塞进 SFX 安装器：流式解压与 minisign 信任根》
  4. 《让 LLM 调用经宿主代理：iframe 永不持凭据的 AI 桥设计》
  5. （D1=A） 《本地优先 RAG 桌面化：从 Qdrant 到插件级检索的取舍》

**拍板结果（2026-08-28）**：平台仅 **GitHub Discussions**（项目内深度文）；节奏 6 个月 4-6 篇、每月约 1 篇、质量优先；首发选题 1《零服务器桌面插件平台：Tauri v2 + Job Object 沙箱的三档安全边界》。（掘金不运营。）

---

## 2. 阶段 P：短期收口（最高优先，前置一切分发）

**目标**：把已登记未派发的 QX-16~QX-19 派发并验收。**这些工单是分发的前置——发版流水线不打通，每个 Release 都是死版，分发无从谈起。**

### P1 · QX-16 发版流水线补强（已登记，待派发）

详见 `docs/WORK_ORDERS.md` QX-16：release workflow 生成并上传 `latest.json` + 安装包 minisign 签名。

**验收**：对测试 tag 实跑一次发版，`latest.json` + `.minisig` 出现在 Release assets；`check_update` 解析有 fixture/单测证明。

### P2 · QX-17 更新链路真机闭环 e2e（已登记，待派发）

详见 `docs/WORK_ORDERS.md` QX-17：环回 update-feed 双向断言（成功闭环 + 篡改拒绝）。

**验收**：双向断言真机绿且有记录；`updater.exe` update 模式首次真机运行的集成缺陷暴露即修。

### P3 · QX-18 安装闭环本机复核（已登记，待派发）

详见 `docs/WORK_ORDERS.md` QX-18：先重建安装器 → `QIANXIA_SETUP_EXE=... pnpm test:install` 真闭环 → `runtime:populate` 远程回退实跑。

**验收**：安装器闭环 exit 0 且无降级标记；populate 远程回退有实证记录。

### P4 · QX-19 双插件导入 + 能力面观察项闭环（已登记，待派发）

详见 `docs/WORK_ORDERS.md` QX-19：clip-digest + web-clip 双 `.qplugin` 桌面壳导入走通 + clipboard 正向往返 + net.fetch 公网正向 + relay-adapter 四 kind 正向 + `is_blocked_host` 真实域名 DNS fail-closed。

**验收**：1-4 有 CDP/脚本证据；5 有单测或豁免记录。

### P5 · P 阶段验收闸门

P1-P4 全部验收通过 + main 同步 + 三基线全绿后，才允许进入阶段 Q-R。**P 不闭环，分发门面与真用插件都建立在沙地上。**

---

## 3. 阶段 Q：分发门面（P5 通过后立即开工，可与 R 并行）

**目标**：让一个陌生人在 GitHub 上点开仓库后，3 分钟内决定"装一个试试"。

### Q1 · README 重写（用户视角，非开发者视角）→ QX-20

**现状**：现有 README 是开发者向（quickstart + 架构地图 + 三档安全），对陌生用户不友好。

**范围**：
- 顶部 3 屏：一句话定位（绑定 D1 选定的垂直场景）→ 1 张 Demo GIF → 一条 `pnpm install && pnpm dev:desktop` 装机命令 → 装好后能看到什么。
- 中段：垂直场景插件的真实使用截图（来自 R1 真用插件）。
- 底部：链接到 CODEBUDDY.md（架构）、docs/plugin-development.md（开发插件）、docs/decisions/（决策记录）。
- 保留现有"零服务器叙事 + 三档安全 + 仓库名说明"内容，但下移到"技术细节"折叠区。

**验收**：README 在 GitHub 渲染 3 屏内说清"是什么 / 为什么 / 怎么装"；包含 Demo GIF 与真实截图。

### Q2 · Demo GIF / 1 分钟视频 → QX-20（同工单）

**范围**：60 秒以内 GIF/MP4，展示 D1 垂直场景插件的真实使用流程（导入 → 运行 → 看到结果）。
- 不做炫技剪辑，纯真实操作录屏；
- 文件 < 5MB（GIF 压缩或 MP4 直接放 release assets 链接）；
- README 顶部嵌入。

### Q3 · Landing page（可选，D2 拍板后定） → QX-21

**范围**（仅当 D2 圈定"必做"）：GitHub Pages 静态页，复用 docs/ 内容构建；一屏定位 + Demo + 装机引导 + 链接到 GitHub。
- 不引入新框架；直接 HTML + Tailwind CDN 或复用 ui-tokens；
- 域名：`<user>.github.io/my-treasure` 或 `qianxia.workbuddy.cn`（如可挂）。

### Q4 · 文档门面校准 → QX-22

**范围**：
- `docs/` 顶层加 `index.md` 作为文档门面（一页索引到所有文档）；
- `docs/getting-started.md`（用户向 5 分钟装机 + 试插件，区别于开发者向的 plugin-development.md）；
- `docs/decisions/` 加 `index.md` 索引到所有 ADR。

**验收**：陌生人从 README 出发，3 跳内能找到装机/用插件/开发插件三类入口。

---

## 4. 阶段 R：真用插件 + dogfoading（D1 定锚后开工，与 Q 并行）

**目标**：选一个垂直场景做深 1 个真用插件，小潘自己每天用，逼出真实摩擦反哺 SDK 与能力面。

### R1 · 真用插件 v1（D1 定锚后实施） → QX-23

**前置**：D1 拍板（强烈建议 A · 本地知识库 / RAG）。

**范围（D1=A 路径，待 D1 拍板后细化）**：
- `packages/plugin-sdk/examples/kb-station/`（client 运行时，新插件）
- 能力面覆盖：`storage.kv`（文档持久化）+ `llm.chat`（摘要/问答）+ `ui.view`（结果展示）+ `fs.read`（导入文档）+ `net.fetch`（可选，拉取在线文档）
- v1 范围红线（建议）：
  - 做：导入 .md/.txt 文档 → 切片 → 关键词全文检索（复用 storage.kv list + 客户端 JS 检索）→ LLM 问答（带检索片段作为 context）
  - 不做：向量检索（v2 范围）、跨插件共享知识层（架构改动过大）、PDF/Office 解析（依赖过重）
- 走完整流程：`plugin:create` → `validate` → `build` → `dev` 热循环 → 本地导入真机跑通。

**验收**：插件真机跑通（CDP 证据）；小潘自己用 ≥1 周每日使用；新增 ≥3 条摩擦记录到 `docs/g2-sdk-friction.md` 第三轮。

### R2 · 摩擦反哺 → QX-24

**范围**：把 R1 dogfoading 暴露的摩擦项逐个评估：
- SDK 层（错误码、超时、API 形状）→ 修；
- 能力面层（限额是否合理、新 op 是否需要）→ 评估 + 单测；
- 文档层（用法不清楚）→ 补。
- 不做"为做而做"的项显式记录理由（延续 J5 模式）。

**验收**：friction 记录新增 ≥3 条；高价值项落地修复；三基线全绿。

### R3 · v1.x 真用插件迭代（持续） → QX-25（如需要）

**范围**：R1 后小潘继续用 ≥2 个月，按真实需求迭代；不预设方向，由 dogfoading 反馈驱动。
- 候选方向（不预设，按反馈）：文档分类、标签、最近列表、全文检索优化、跨笔记链接。
- 每个迭代走完整流程并记 friction。

**验收**：每个迭代真机跑通 + friction 记录更新。

---

## 5. 阶段 S：真实用户冷启动（R1 通过后启动，与 R3 并行）

**目标**：从 0 到 1 找到 3-5 名真实种子用户，建立反馈循环；6 个月末达到"有真实周活用户"状态。

### S1 · 种子用户触达 → QX-26

**前置**：D3 拍板（触达渠道 + 反馈通道）+ R1 通过（产品能演示）。

**范围**：
- 按 D3 圈定渠道发"求内测"帖（掘金/V2EX/即刻/GitHub Discussions）；
- 帖子内容模板：一句话定位 + Demo GIF + 装机指引 + 内测群入口 + 反馈通道；
- 私聊触达 ≥10 人，目标转化 3-5 名种子。

**验收**：3-5 名种子用户加入反馈通道；至少 1 名完成"装好 + 跑通一个插件"流程。

### S2 · 反馈循环与摩擦记录 → QX-27

**范围**：
- 每周复盘一次 Issues / 群反馈；
- 把用户反馈转成 friction 记录或工单；
- 暴露的真实缺陷优先级高于 R3 自驱迭代。

**验收**：每周有反馈复盘记录；用户反馈驱动的修复 ≥3 项落地。

### S3 · 公开 Release（v0.1.0） → QX-28

**前置**：S1 通过 + R1 通过 + Q1/Q2 通过 + P5 通过。

**范围**：
- 第一个面向公众的 GitHub Release（v0.1.0）；
- Release notes 含：定位 + Demo GIF 链接 + 装机指引 + 已知限制（v1 Windows-only / 无市场 / 无自动更新后台下载）；
- 配套发布 D4 选题 1 的架构博客（掘金 + GitHub Discussions）。

**验收**：v0.1.0 Release 上线；Release notes 完整；架构博客首发。

### S4 · 内容输出持续（与 S3 同步启动，覆盖 M3-M6） → QX-29

**范围**：按 D4 节奏持续输出博客（4-6 篇 / 6 个月）。
- 每篇博客发布后同步到 GitHub Discussions + README 链接更新；
- 每篇博客结尾链接到 GitHub 仓库，引导 stars。

**验收**：6 个月末发布 ≥4 篇博客；每篇有可量化的浏览/stars 转化数据。

---

## 6. 阶段 T：求职作品沉淀（贯穿 P-S，独立工单池）

**目标**：把项目从"代码能跑"沉淀成"能讲清为什么这么做的故事"。

### T1 · 架构博客系列（与 S4 同源） → 已在 QX-29

### T2 · 项目深度叙事文档 → QX-30

**范围**：`docs/PROJECT_NARRATIVE.md`（新文档，求职用）——
- 一页讲清：项目定位、核心架构决策（4-5 个）、技术深度亮点（求职向量化指标）、6 个月发展轨迹、未来方向。
- 不写营销话术，纯工程叙事；
- 面试官 5 分钟读完能问出深度问题。

**验收**：文档存在；含 4-5 个架构决策的"为什么这么做"理由；含可量化指标（测试数、Runtime 物料大小、冷启动时间、单插件成本等）。

### T3 · README stars 与可量化指标 → QX-31

**范围**：
- README 加 "Metrics" 段（GitHub stars / clones / Release downloads，用 shields.io badges）；
- 6 个月末目标：stars ≥200、Release 下载 ≥500、至少 1 篇博客上掘金首页；
- 不刷 stars，靠内容质量与垂直场景叙事吸引。

**验收**：README 含 metrics badges；6 个月末达到 stars ≥200（或如实标注未达原因）。

---

## 7. 工单草案（✅ D1-D4 已拍板 2026-08-28，已注册到 WORK_ORDERS.md 第六轮工单节）

| 工单 | 阶段 | 内容 | 依赖 | 状态 |
|---|---|---|---|---|
| QX-20 | Q1+Q2 | README 重写 + Demo GIF | P5；R1 截图 | 已注册待派发 |
| QX-21 | Q3 | Landing page（D2 拍板=可选） | QX-20 | 可选，暂不派发 |
| QX-22 | Q4 | 文档门面校准（index.md / getting-started.md） | QX-20 | 已注册待派发 |
| QX-23 | R1 | 真用插件 v1（D1=A 路径：本地知识库 / RAG） | D1 拍板 ✅ | 已注册待派发 |
| QX-24 | R2 | 摩擦反哺 SDK/能力面 | QX-23 | 已注册待派发 |
| QX-25 | R3 | v1.x 真用插件迭代（如需要） | QX-23 | 已注册待派发 |
| QX-26 | S1 | 种子用户触达（渠道=D3 ✅ 同学/朋友圈） | D3 拍板 ✅ + QX-23 | 已注册待派发 |
| QX-27 | S2 | 反馈循环与摩擦记录 | QX-26 | 已注册待派发 |
| QX-28 | S3 | 公开 Release v0.1.0 | P5 + QX-23 + QX-20 | 已注册待派发 |
| QX-29 | S4 | 内容输出持续（4-6 篇博客，平台=D4 ✅ 仅 Discussions） | D4 拍板 ✅ | 已注册待派发 |
| QX-30 | T2 | 项目深度叙事文档 | QX-23 落地后 | 已注册待派发 |
| QX-31 | T3 | README metrics badges | QX-28 | 已注册待派发 |

**注**：QX-16~QX-19 已登记于 `docs/WORK_ORDERS.md` 第五轮工单节，本轮重新定位为阶段 P 前置；不再重复登记。

---

## 8. 建议执行顺序（✅ D1-D4 已拍板 2026-08-28，按此执行）

1. **M1（约 3 周）**：~~拍板 D1-D4~~（✅ 已完成）→ 派发 QX-16~QX-19（阶段 P 全收口）；
2. **M2（约 4 周）**：QX-16~19 验收 → 启动 R1（真用插件 v1，D1=A）+ Q1/Q2（README + Demo）并行；
3. **M3-M4（约 8 周）**：R1 dogfoading → R2 摩擦反哺 → Q3/Q4 文档门面（Q3 可选暂缓）→ S1 种子用户触达；
4. **M5（约 4 周）**：S2 反馈循环 → S3 公开 Release v0.1.0 → S4 内容输出启动（仅 Discussions）；
5. **M6（约 4 周）**：S4 持续内容输出 → T2 项目叙事 → T3 metrics → 6 个月末复盘。

**总闸**：D1-D4 已拍板 ✅，阶段 Q-R-S-T 按依赖顺序放行；阶段 P 先行（P 不依赖战略拍板）。

---

## 9. 不做的事（边界守护，延续 H2/H3 + 本轮约束）

| 项 | 理由 |
|---|---|
| 不引入轻量云 / 不建市场 / 不做计费 | 违背约束 2（求职作品+个人工具）；零服务器是灵魂 |
| 不做跨平台（mac/Linux） | 违背约束 1（3-6 个月）；H2 ADR 已记录硬骨头清单 |
| 不扩能力面（17 kind 之外） | 延续 H3；新 kind 直到真实插件需求出现 |
| 不刷 stars / 不付费投放 | 靠内容质量与垂直场景叙事；约束 2 不追求商业化 |
| 不引入新基础设施（独立文档站/CI 复杂化/新框架） | 延续方针"停止追加基础设施"；6 个月见效要求投资克制 |
| `plugin_script.rs` 硬隔离 TODO 不在本轮范围 | 独立大任务，刻意推迟，3-6 个月内不动 |
| CSP 收紧（K2/F4） | 维持触发条件：下次实质改 `PluginRunner.tsx` 渲染路径时再动 |
| 静态插件免 tsx（J5） | 已记录不做；tsx 冷启动 1-2s 可接受 |

---

## 10. 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| D1 选错垂直场景，dogfoading 自己都不想用 | 中 | 高 | 选 A（RAG）咬合小潘技术主线 + 已有 KB-AI 经验，风险最低 |
| 3-6 个月时间不够，公开 Release 滑期 | 中 | 中 | S3 不做硬截止；若 M5 末未达，延后到 M6 末或 v0.1.0-rc 标签 |
| 种子用户转化率低（<3 人） | 中 | 中 | 多渠道触达（同学/技术圈/GitHub followers）；降低门槛（Demo + 一键装机） |
| R1 真用插件范围失控（RAG 深坑） | 高 | 高 | v1 严格红线（不做向量检索 / 跨插件共享 / PDF 解析）；按 dogfoading 反馈渐进加 |
| 求职面试官听不懂"插件平台"叙事 | 中 | 高 | T2 项目叙事 + D1 场景定锚解决；博客系列把架构决策讲清 |
| 内容输出挤压代码时间 | 中 | 中 | 节奏 1 篇/月，质量优先；博客本身就是沉淀，不是额外负担 |

---

## 附：本规划与前四轮的关系

- **前四轮**（A-O + QX-01~14）：建基础设施、打通链路、补能力面、制度化验证、收口文档——**已交付**。
- **第四轮工单池**（QX-16~19）：发版流水线 + 更新闭环 + 安装闭环 + 观察项收口——**已登记未派发**，本轮重新定位为**阶段 P 前置**。
- **本轮**（P-T + QX-20~31）：从"代码能跑"走向"真实用户在用 + 求职可讲故事"——**待小潘拍板 D1-D4 后启动**。
- **下一轮预告**（v2 候选方向，3-6 个月外）：向量检索 / 跨插件共享知识层 / 跨平台评估 / 插件市场评估——**本轮不做**，仅记录候选供未来评估。
