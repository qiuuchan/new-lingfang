# 运行时分片二进制走 Git LFS，避免 285MB+ chrome.dll 分片拖垮克隆（B3 决策）。
apps/desktop/runtime-parts/** filter=lfs diff=lfs merge=lfs -text
*.part-* filter=lfs diff=lfs merge=lfs -text

# latest.json fixture 参与逐字节漂移防护（LF-16），任何平台检出都必须保持 LF。
scripts/fixtures/latest.json text eol=lf
